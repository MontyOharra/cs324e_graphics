class DefeatScreen {
  GameManager manager;
  PVector windowSize;
  
  DefeatScreen(GameManager manager) {
    this.manager = manager;
    this.windowSize = manager.windowSize;
  }
  
  void render() {}
  void mousePressed() {}
}
